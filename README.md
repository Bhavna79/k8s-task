# Kubernetes Minikube Task

This project demonstrates deploying a simple app on Kubernetes using Minikube.

## Folder Structure
## Prerequisites

- [Minikube](https://minikube.sigs.k8s.io/)
- [kubectl](https://kubernetes.io/docs/tasks/tools/)
- Docker (Windows users need it as the driver for Minikube)

## Steps to Run

### 1. Start Minikube
```bash
minikube start
---

### *Step 3: Save the File*

VS Code ho ya Notepad — README.md ko save kar le.

---

### *Step 4: GitHub Pe Upload Karna*

1. GitHub pe ek *naya repository* bana: e.g. k8s-minikube-task.
2. Ab terminal ya Git Bash me yeh commands chala:

```bash
cd ~/Desktop/k8s-task     # Folder me jaa
git init
git add .
git commit -m "Initial commit with YAMLs and README"
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin master